= Управление карточкой

Элемент управления `_Управление карточкой_` предназначен для выполнения с карточкой стандартных действий: открытие для редактирования, обновление и удаление.

.Элемент управления "Управление карточкой" в открытой карточке
image::card-management.png[Элемент управления "Управление карточкой" в открытой карточке]

Права на редактирование проверяются по доступности _операции редактирования_, выбранной в соответствующем свойстве элемента управления. Права на удаление проверяются по встроенной операции удаления. Значки недоступных операций не отображаются в элементе управления.

NOTE: Данный ЭУ может быть добавлен только в разметку, предназначенную для просмотра карточек.

== Свойства

include::partial$properties.adoc[tags=general]

=== Данные

==== Операция редактирования

Выбор операции для редактирования карточки. Если значение не задано, кнопка редактирования будет отображаться всем.

Если значение `*Операции редактирования*` наследуется от родительского Блока, название свойства меняется на `*Операция редактирования (наследовано)*`.

=== Поведение

// ==== Дублирование
//
// Свойство регулирует доступность значка дублирования в элементе управления.
//
// .Доступны для выбора варианты:
// * *_Запрещено_* -- дублирование карточки запрещено, значение по умолчанию.
// * *_Открыть в разметке на просмотр_* -- дублирование карточки разрешено, дубликат будет открыт в разметке на просмотр.
// * *_Открыть в разметке на создание_* -- дублирование карточки разрешено, дубликат будет открыт в разметке создания.
//
// ==== При дублировании открывать
//
// Свойство определяет, как будет открыт дубликат карточки после его создания:
//
// * *_В новой вкладке_* -- значение по умолчанию.
// * *_В текущей вкладке_*. Если в свойстве `*Дублирование*` выбран вариант *_Запрещен_*, данное свойство нельзя редактировать.
//
// include::partial$properties.adoc[tags=folder-select-window]

include::partial$properties.adoc[tags=visibility]

include::partial$properties.adoc[tags=addCss]

include::partial$properties.adoc[tags=disabled]

include::partial$properties.adoc[tags=stdCss]

=== События

include::partial$properties.adoc[tags=mouseOver]

include::partial$properties.adoc[tags=mouseAway]

include::partial$properties.adoc[tags=onClick]

// ==== Перед дублированием
//
// Вызывается перед дублированием карточки.
//
// ==== После дублирования
//
// Вызывается после дублирования карточки.
