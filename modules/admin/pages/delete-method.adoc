= Изменить способ удаления карточек и папок

По умолчанию карточки и папки из {wc}а удаляются в корзину. Администратор может включить режим удаления без возможности восстановления.

[NOTE]
====
Восстановить удалённые карточки и папки можно только в {wincl}е.
====

// tag::webconfig[]
. Откройте конфигурационный файл `{webconfig}`.
. Перейдите к секции {cnf-sgrps-wc}].
. Измените значение параметра `DeletePermanently`:
+
.Удалённые карточки и папки будут помещаться в корзину
[source,json]
----
  "Docsvision": {
    "WebClient": {
      "SettingGroups": {
        "WebClient": {
          "DeletePermanently": "false" <.>
        }
      }
    }
  }
----
<.> Возможные значения:
+
* `True` -- отключает использование корзины.
* `False` -- удалённые карточки и папки помещаются в корзину (значение по умолчанию).
// end::webconfig[]
+
include::partial$excerpts.adoc[tags=ifabsent]
+
. Сохраните изменения конфигурационного файла.
